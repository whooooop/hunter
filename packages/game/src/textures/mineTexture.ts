import { ImageTexture } from "../types/texture";

import MineTextureUrl from './assets/mine_texture_0.png';

export const MineImageTexture_0: ImageTexture = {
  key: 'mine_texture_0',
  url: MineTextureUrl,
  scale: 0.5,
}