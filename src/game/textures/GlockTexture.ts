import { ImageTexture } from "../core/types/texture";

import GlockTextureUrl from './assets/glock_texture_0.png';

export const GlockImageTexture_0: ImageTexture = {
  key: 'glock_texture_0',
  url: GlockTextureUrl,
  scale: 0.5,
} 