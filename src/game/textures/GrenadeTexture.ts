import { ImageTexture } from "../core/types/texture";

import GrenadeTextureUrl from './assets/grenade_texture_0.png';

export const GrenadeImageTexture_0: ImageTexture = {
  key: 'grenade_texture_0',
  url: GrenadeTextureUrl, 
  scale: 0.5,
} 