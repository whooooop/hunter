import { ImageTexture } from "../core/types/texture";

import SawedTextureUrl from './assets/sawed_texture_0.png';

export const SawedImageTexture_0: ImageTexture = {
  key: 'sawed_texture_0',
  url: SawedTextureUrl, 
  scale: 0.5,
} 